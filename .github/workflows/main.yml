name: Build OpenWrt NN6000v2

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/build-nn6000v2.yml'
      - 'configs/**'

jobs:
  build:
    name: Build NN6000v2 (IPQ6000 / qualcommax)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Free disk space
        run: |
          sudo rm -rf /usr/share/dotnet /opt/ghc \
            /usr/local/lib/android /usr/local/share/boost \
            /usr/lib/jvm
          df -h

      - name: Install build dependencies
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential clang flex bison g++ gawk \
            gcc-multilib g++-multilib gettext git \
            libncurses5-dev libssl-dev python3-dev python3-setuptools \
            rsync unzip zlib1g-dev file wget

      - name: Clone OpenWrt openwrt-25.12 branch
        run: |
          git clone --depth 1 \
            -b openwrt-25.12 \
            https://github.com/openwrt/openwrt.git openwrt

      - name: Update & install feeds
        working-directory: openwrt
        run: |
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Copy .config
        run: cp configs/nn6000v2.config openwrt/.config

      - name: Expand config defaults
        working-directory: openwrt
        run: make defconfig

      - name: Cache downloads
        uses: actions/cache@v4
        with:
          path: openwrt/dl
          key: openwrt-25.12-dl-${{ hashFiles('openwrt/.config') }}
          restore-keys: openwrt-25.12-dl-

      - name: Download package sources
        working-directory: openwrt
        run: make download -j$(nproc) V=s 2>&1 | grep -E "^(ERROR)" || true

      - name: Build firmware
        working-directory: openwrt
        run: |
          make -j$(nproc) V=s 2>&1 | tee build.log || \
          make -j1 V=s 2>&1 | tee build.log

      - name: Upload images
        uses: actions/upload-artifact@v4
        with:
          name: nn6000v2-25.12-${{ github.run_number }}
          path: |
            openwrt/bin/targets/qualcommax/ipq60xx/*nn6000v2*factory*.bin
            openwrt/bin/targets/qualcommax/ipq60xx/*nn6000v2*sysupgrade*.bin
            openwrt/bin/targets/qualcommax/ipq60xx/*nn6000v2*initramfs*.itb
            openwrt/bin/targets/qualcommax/ipq60xx/sha256sums
          retention-days: 14

      - name: Upload build log on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-log
          path: openwrt/build.log
